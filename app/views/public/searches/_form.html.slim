= search_form_for  url: search_path do |f|
  = f.text_field :title_or_body_cont
  = f.select :model, options_for_select({ "User" => "user", "Book" => "book" })
  = f.submit '検索', class: 'btn btn-primary'